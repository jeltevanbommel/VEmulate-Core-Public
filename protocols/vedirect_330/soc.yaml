name: State-of-charge
key: SOC
unit: ‰
default: 0
values:
  - type: IntRange
    min: 0
    max: 1000

detailed_description: "State-of-charge"

custom_gui_elements:
  - type: range
    min: 0
    max: 1000