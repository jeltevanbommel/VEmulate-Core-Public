name: Load current
key: IL
unit: mA
default: 0
values:
  - type: IntRange
    min: -10000
    max: 10000

detailed_description: "Available since version v1.15, and only for models with a load output."

custom_gui_elements:
  - type: range
    min: -10000
    max: 10000